package BankAccount;

import java.util.ArrayList;

public class Bank {
	private ArrayList<Account> accounts;
	
	public void addAccount(Account account, int accountNum){
		account.setAccountNum(accountNum);
		accounts.add(account);
	}
	
	public void removeAccount(int accountNum){
		int i = 0;
		for(Account a: accounts){
			if(a.getAccountNum() == accountNum){
				accounts.remove(i);
			}
			i++;
		}
	}
	public void addMoney(int accountNum, float amount){
		int i = 0;
		for(Account a: accounts){
			if(a.getAccountNum() == accountNum){
				a.addMoney(amount);
			}
		}
		i++;
	}
	public void widthdrawMoney(int accountNum, float amount) throws AccountOverdrawnException{
		int i = 0;
		for(Account a: accounts){
			if(a.getAccountNum() == accountNum){
				a.widthdrawMoney(amount);
			}
		}
	}
}
